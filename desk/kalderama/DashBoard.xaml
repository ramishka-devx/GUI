<Window x:Class="kalderama.DashBoard"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:lc="clr-namespace:LiveCharts.Wpf;assembly=LiveCharts.Wpf"
        Title="Dashboard" Height="450" Width="800">
    <Grid>
        <TextBlock Name="txtTotalOrders" HorizontalAlignment="Center" VerticalAlignment="Top" Margin="10" FontSize="16"/>
        <TextBlock Name="txtTotalRevenue" HorizontalAlignment="Center" VerticalAlignment="Top" Margin="10,40,10,10" FontSize="16"/>

        <!-- Button to load graph -->
        <Button Name="btnLoadGraph" Width="100" Height="30" HorizontalAlignment="Center" VerticalAlignment="Top" Margin="0,80,0,0" Content="Load Graph" Click="btnLoadGraph_Click"/>

        <!-- Chart control -->
        <lc:CartesianChart Name="revenueChart" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Margin="10,120,10,10">
            <lc:CartesianChart.Series>
                <!-- Example of adding a line chart -->
                <lc:LineSeries Title="Revenue" Values="{Binding RevenueValues}" PointGeometrySize="15" StrokeThickness="2"/>
            </lc:CartesianChart.Series>
        </lc:CartesianChart>
    </Grid>
</Window>
